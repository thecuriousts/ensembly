---
name: goal-driven-hitl-spine-iteration
description: Seed todos from goal, implement core flows, fix skeptic-flagged gates, run full test suite, then finalize with git
kind: workflow
skill_chain: ["todo-seed-from-goal", "approval-gate-mapping", "circular-import-break", "git-tag-commit-push"]
---

# goal-driven-hitl-spine-iteration

Seed todos from goal, implement core flows, fix skeptic-flagged gates, run full test suite, then finalize with git

## Skill chain

1. `todo-seed-from-goal`
2. `approval-gate-mapping`
3. `circular-import-break`
4. `git-tag-commit-push`

## Phases

### Explore

Load goal, map kernel modules, seed todos

### Implement

Build projection/graph/HITL flows and break circular deps

### Harden

Fix approval state mapping and re-activation logic

### Finalize

Tag, commit, push

## Support

- sessions: 1
- rank: 29
