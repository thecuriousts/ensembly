# Workspace root — operator kernel (Rust only at repo root).
[workspace]
members = [
    "crates/peram-kernel",
    "crates/peram-memory",
    "crates/peram-agents",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
version = "0.3.0"
