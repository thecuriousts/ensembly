# Workspace root — operator kernel (Rust only at repo root).
[workspace]
members = [
    "crates/ensembly-kernel",
    "crates/ensembly-memory",
    "crates/ensembly-agents",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
version = "0.3.0"
