# Dependencies & build
node_modules/
coverage/
build/
dist/
stats.json
npm-debug.log*
.DS_Store
.idea/

# --- Privacy contract: local-only (never push) ---
# Full operator persona, private actions, local state, copied life data
private/
data/
data/local/
data/private/
*.local.json
*.private.md
*.private.json
.env
.env.*
!.env.example

# Operator life-derived game graph (prototype CLI export; sample-graph is committed fallback)
prototype/public/game/life-graph.json

# Agent / scratch (local only)
.agents/
.claude/
.scratch/
tmp/

# Grok CLI project config (operator-generated via `grok mcp add`; never commit keys)
.grok/

# Rust build artifacts
target/
crates/**/target/
prototype/crates/**/target/
**/*.rs.bk
